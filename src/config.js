const http = 'https'
const host = 'dev-philips.wemartx.com'
const appID = 'wx63997dce5835a6c2'
const appKey = ''
const shopId = '139'
const isUseSearchEngine = false
const config = {
  http,
  host,
  appID,
  appKey,
  shopId,
  isUseSearchEngine
}
export default config
